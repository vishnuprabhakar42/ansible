##Adding User to the Playbook 
    - adding user so that everything is done through that user not through root user 
    
##Steps
    - Create a new user 
    - Run app using that new user 

##For running the playbook 
    ansible-playbook -i hosts deploy-node.yaml




#Variables
